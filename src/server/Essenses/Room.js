class Room {
    constructor(data) {
        this.id = Math.random();
        this.creator = data._id;
        this.participantsTokens = [/* tokens */];
    }

    getId() {
        return this.is;
    }
}

module.exports = Room;